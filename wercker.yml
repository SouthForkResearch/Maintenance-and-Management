box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.15"
        theme: nardocs
        flags: --buildDrafts=true --verbose

deploy: 
  steps:
    - s3sync:
        key_id: $KEY
        key_secret: $SECRET
        bucket_url: $URL
        source_dir: public/